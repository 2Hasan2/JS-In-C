#ifndef ARRAY_UTILS_H
#define ARRAY_UTILS_H

// Function to add an element to the end of an array
void push(int *arr, int element, int *size);

// Function to remove and return the last element of an array
int pop(int *arr, int *size);

// Declare other array functions...

#endif
